---
import { getCollection, type CollectionEntry } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import BlogNavbar from '../components/blog/BlogNavbar.astro';
import { calculateReadingTime } from '../utils/readingTime';

type BlogPost = CollectionEntry<'blog'>;

// Get all non-draft posts, sorted by date (newest first)
const posts: BlogPost[] = (await getCollection('blog', ({ data }: BlogPost) => !data.draft))
  .sort((a: BlogPost, b: BlogPost) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf());
---

<BaseLayout title="Blog | Bryce Keeler" description="Thoughts on software engineering, ML, home labs, and other things I find interesting.">
  <BlogNavbar />

  <main class="blog-page">
    <header class="blog-header">
      <h1>Blog</h1>
      <p class="blog-subtitle">Thoughts on software, ML, home labs, and whatever else I find interesting</p>
    </header>

    <section class="blog-posts">
      {posts.length === 0 ? (
        <div class="blog-empty-state">
          <p>No posts yet. Check back soon.</p>
        </div>
      ) : (
        posts.map((post: BlogPost) => {
          const readingTime = calculateReadingTime(post.body);
          const formattedDate = post.data.publishDate.toLocaleDateString('en-US', {
            year: 'numeric',
            month: 'long',
          });

          return (
            <a href={`/blog/${post.slug}`} class="blog-post-link">
              <article class="blog-post">
                <div class="post-meta-row">
                  <span class="post-date">{formattedDate}</span>
                  <span class="post-reading-time">{readingTime}</span>
                </div>
                <h2 class="post-title">{post.data.title}</h2>
                <p class="post-excerpt">{post.data.description}</p>
                <div class="post-tags">
                  {post.data.tags.map((tag: string) => (
                    <span class="post-tag">{tag}</span>
                  ))}
                </div>
              </article>
            </a>
          );
        })
      )}
    </section>

    <section class="subscribe-section">
      <h2>Stay Updated</h2>
      <p>Get notified when I publish new posts. No spam, unsubscribe anytime.</p>
      <form class="subscribe-form" action="https://buttondown.com/api/emails/embed-subscribe/BryceKeeler" method="POST" target="_blank">
        <input
          type="email"
          name="email"
          placeholder="your@email.com"
          required
          class="subscribe-input"
        />
        <button type="submit" class="subscribe-btn">Subscribe</button>
      </form>
      <p class="rss-link">
        Or subscribe via <a href="/rss.xml">RSS feed</a>
      </p>
    </section>
  </main>

  <footer class="blog-footer">
    <div class="footer-content">
      <p>Bryce Keeler · 2026</p>
      <div class="footer-links">
        <a href="/">Home</a>
        <span>·</span>
        <a href="/traditional">Traditional View</a>
        <span>·</span>
        <a href="/traditional#contact">Contact</a>
      </div>
    </div>
  </footer>
</BaseLayout>

<style>
  .blog-page {
    min-height: 100vh;
    padding: 8rem 2rem 4rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .blog-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .blog-header h1 {
    font-size: clamp(2.5rem, 6vw, 3.5rem);
    font-weight: 800;
    color: var(--text-primary);
    margin-bottom: 1rem;
  }

  .blog-subtitle {
    font-size: 1.125rem;
    color: var(--text-secondary);
    max-width: 500px;
    margin: 0 auto;
    line-height: 1.6;
  }

  .blog-posts {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
  }

  .blog-post-link {
    text-decoration: none;
    display: block;
  }

  .blog-post {
    background: rgba(220, 215, 186, 0.03);
    border: 1px solid rgba(220, 215, 186, 0.1);
    border-radius: 16px;
    padding: 2rem;
    transition: all 0.3s ease;
  }

  .blog-post:hover {
    border-color: rgba(220, 215, 186, 0.2);
    background: rgba(220, 215, 186, 0.05);
  }

  .post-meta-row {
    display: flex;
    gap: 1.5rem;
    margin-bottom: 0.75rem;
  }

  .post-date,
  .post-reading-time {
    font-size: 0.875rem;
    color: var(--text-secondary);
  }

  .post-title {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: 1rem;
  }

  .post-excerpt {
    font-size: 1rem;
    color: var(--text-secondary);
    line-height: 1.7;
    margin-bottom: 1rem;
  }

  .post-tags {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .post-tag {
    font-size: 0.8125rem;
    padding: 0.25rem 0.75rem;
    background: rgba(220, 215, 186, 0.05);
    border: 1px solid rgba(220, 215, 186, 0.1);
    border-radius: 4px;
    color: var(--text-secondary);
  }

  .blog-empty-state {
    text-align: center;
    padding: 3rem 2rem;
    color: var(--text-secondary);
  }

  .blog-footer {
    padding: 3rem 2rem;
    border-top: 1px solid rgba(220, 215, 186, 0.1);
    background: rgba(31, 31, 40, 0.5);
  }

  .footer-content {
    max-width: 800px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    text-align: center;
  }

  .footer-content p {
    color: var(--text-secondary);
    font-size: 0.9375rem;
  }

  .footer-links {
    display: flex;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .footer-links a {
    color: var(--text-secondary);
    text-decoration: none;
    font-size: 0.875rem;
    transition: color 0.3s ease;
  }

  .footer-links a:hover {
    color: var(--primary-color);
  }

  .footer-links span {
    color: rgba(220, 215, 186, 0.2);
  }

  .subscribe-section {
    margin-top: 4rem;
    padding: 3rem;
    background: rgba(220, 215, 186, 0.03);
    border: 1px solid rgba(220, 215, 186, 0.1);
    border-radius: 16px;
    text-align: center;
  }

  .subscribe-section h2 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: 0.5rem;
  }

  .subscribe-section > p {
    color: var(--text-secondary);
    margin-bottom: 1.5rem;
  }

  .subscribe-form {
    display: flex;
    gap: 0.75rem;
    max-width: 400px;
    margin: 0 auto 1rem;
  }

  .subscribe-input {
    flex: 1;
    padding: 0.875rem 1rem;
    background: rgba(220, 215, 186, 0.05);
    border: 1px solid rgba(220, 215, 186, 0.2);
    border-radius: 8px;
    color: var(--text-primary);
    font-size: 0.9375rem;
    font-family: inherit;
  }

  .subscribe-input::placeholder {
    color: var(--text-secondary);
  }

  .subscribe-input:focus {
    outline: none;
    border-color: rgba(220, 215, 186, 0.4);
  }

  .subscribe-btn {
    padding: 0.875rem 1.5rem;
    background: rgba(220, 215, 186, 0.1);
    border: 1px solid rgba(220, 215, 186, 0.2);
    border-radius: 8px;
    color: var(--text-primary);
    font-size: 0.9375rem;
    font-weight: 600;
    font-family: inherit;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .subscribe-btn:hover {
    background: rgba(220, 215, 186, 0.15);
    border-color: rgba(220, 215, 186, 0.4);
  }

  .rss-link {
    font-size: 0.875rem;
    color: var(--text-secondary);
  }

  .rss-link a {
    color: var(--text-secondary);
    text-decoration: underline;
    text-underline-offset: 2px;
    transition: color 0.2s ease;
  }

  .rss-link a:hover {
    color: var(--text-primary);
  }

  @media (max-width: 1024px) {
    .blog-posts {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .blog-page {
      padding: 6rem 1.5rem 3rem;
    }

    .blog-posts {
      grid-template-columns: 1fr;
    }

    .blog-post {
      padding: 1.5rem;
    }

    .post-meta-row {
      flex-direction: column;
      gap: 0.25rem;
    }

    .footer-links {
      flex-direction: column;
      gap: 0.5rem;
    }

    .footer-links span {
      display: none;
    }

    .subscribe-section {
      padding: 2rem 1.5rem;
    }

    .subscribe-form {
      flex-direction: column;
    }

    .subscribe-btn {
      width: 100%;
    }
  }
</style>
